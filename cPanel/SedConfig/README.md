#SedConfig
This playbook is used to update a configuration file on all a list of servers.
In this example, we are forcing HTML emails on all RoundCube clients on the shared hosting environment.
In Ansible, single quotes 'like this' has to be escaped ''like this'' so you will see this being used for the playbook to work.
